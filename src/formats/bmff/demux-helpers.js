import {
  parseDescriptors as parseDescriptors_,
  findBox as findBox_,
  findNamedBox as findNamedBox_,
  buildFrameTable as buildFrameTable_,
  addSampleDescription as addSampleDescription_,
  parseTracks as parseTracks_,
  parseMediaInfo as parseMediaInfo_
} from '@videojs/vhs-utils/cjs/mp4-helpers';

export const parseDescriptors = parseDescriptors_;
export const findBox = findBox_;
export const findNamedBox = findNamedBox_;
export const buildFrameTable = buildFrameTable_;
export const addSampleDescription = addSampleDescription_;
export const parseTracks = parseTracks_;
export const parseMediaInfo = parseMediaInfo_;
